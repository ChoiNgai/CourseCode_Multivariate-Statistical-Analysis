%% 第二章第五节 参数估计

%% 生成服从多元正态分布的样本
mu = [0 0];             %均值
sigma = [1,0.9;0.9 1];  %协方差矩阵
n = 100;                 %样本个数
R = mvnrnd(mu,sigma,n);
% plot(R,'rs-')

%% 均值向量估计
mean(R)

%% 协方差估计
sum((R(:,1) - mean(R(:,1))) .* (R(:,2) - mean(R(:,2)))) / (size(R(:,1), 1) - 1)